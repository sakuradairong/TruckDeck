{
  "name": "truckdeck",
  "version": "1.0.0",
  "private": true,
  "description": "局域网手机横屏控制 ETS2/ATS — Express + WebSocket 服务",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node server/scripts/build.js",
    "start": "npm run build && node server/index.js",
    "start:raw": "node server/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "trucksim-telemetry": "^1.0.0"
  }
}
